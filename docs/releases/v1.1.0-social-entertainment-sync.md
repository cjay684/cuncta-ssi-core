# v1.1.0-testnet - Social + Entertainment + Sync Vertical

## Summary

- Social v0.5 spaces-first + governance UX.
- Social v0.4 flow feed + trust lens + explain (non-intrusive).
- Social v0.5 spaces deepening (space flow + governance + moderation audit + analytics).
- Entertainment v0.1 (emoji/sound/presence/watch control-plane).
- Sync v0.2 (scroll + listen control-plane + ephemeral permissions).

## Security Posture Preserved

- SSI invariants unchanged (SD-JWT/KB-JWT/pinning/floor/revocation/DSR/anchoring).
- No custodial keys.
- User-pays first-class, no sponsor dependency.
- SSRF protections intact.
- Strict DB roles + migration separation intact.

## Test Evidence

- pnpm -r build PASS
- pnpm -r typecheck PASS
- pnpm -r test PASS
- RUN_TESTNET_INTEGRATION=1 SOCIAL_MODE=1 pnpm verify:testnet PASS

## Known Non-Blockers

- Testnet eventual consistency handled by bounded, observable integration waits + diagnostics (no semantic change).
