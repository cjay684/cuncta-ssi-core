{
  "name": "@cuncta/app-gateway",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "lint": "eslint \"src/**/*.ts\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --loader ts-node/esm src/test/config.secret-length.development.test.js && node --loader ts-node/esm src/test/config.secret-length.production.test.js && node --loader ts-node/esm src/test/gateway.test.js && node --loader ts-node/esm src/test/ip-allowlist.test.js"
  },
  "dependencies": {
    "@cuncta/db": "workspace:*",
    "@cuncta/shared": "workspace:*",
    "@fastify/rate-limit": "^10.3.0",
    "@hashgraph/sdk": "^2.80.0",
    "dotenv": "^17.2.3",
    "fastify": "^5.7.2",
    "jose": "^6.1.3",
    "zod": "^4.3.6"
  }
}
